"""
AyurKosha — Context Compression
Extracts only relevant sentences from each chunk to maximize info density.

Build this in: Phase 7 (lower priority optimization)
"""

# TODO: For each expanded chunk, use a lightweight Claude call
#   to extract only sentences relevant to the query.
#   This lets you fit more useful info in the context window.
