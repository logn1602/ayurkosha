"""
AyurKosha — Document Loaders
Reads PDFs, DOCX, MD, HTML into raw text + metadata.

Build this in: Phase 2, Step 1
"""

# TODO: Implement loaders for each document format
#   - PDF:  Use pypdf or unstructured
#   - DOCX: Use python-docx
#   - MD:   Use langchain UnstructuredMarkdownLoader
#   - HTML: Use beautifulsoup4
#
# Each loader should return a list of dicts:
#   {"text": "...", "metadata": {"source": "...", "page": N, ...}}
