"""
AyurKosha — BM25 Index Management
Builds, serializes, and loads the BM25 keyword index.

Build this in: Phase 2, Step 7
"""

# TODO: Implement BM25 index management
#   - build_index() — tokenize all chunks, create BM25Okapi
#   - save_index() — pickle to data/cache/bm25_index.pkl
#   - load_index() — load from pickle on server start
#   - Uses tokenize_ayurvedic() from src/retrieval/tokenizer.py
